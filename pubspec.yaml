name: baseproject
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  # libreria di debug per errori di formattazione etc.. - linting- 

flutter:
  uses-material-design: true

  assets:
    # - assets/lottie/
    - assets/img/
